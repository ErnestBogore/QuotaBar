# Sanitized fixtures

Fixtures contain synthetic timestamps, quota percentages, model labels, and token counters only. They must never contain prompts, responses, tool output, credentials, usernames, or repository paths.

`classic-window.json` verifies the activity-anchored five-hour window and the community-reconstructed 6.25× relationship.
