# QuotaBar website

The source for [quotabar.fyi](https://quotabar.fyi). It is a small public landing page built with React and vinext and deployed through OpenAI Sites.

## Local preview

```sh
npm install
npm run dev
```

Run `npm run lint` and `npm run build` before publishing.
